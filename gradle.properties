# Done to increase the memory available to gradle.
org.gradle.jvmargs=-Xmx2G
org.gradle.parallel=true

# Fabric Properties
# 目标版本：1.21.1（项目定位版本，目录名也是 1.21.1）。
# 0.16.0 移除世界空间字幕（WorldRenderEvents）后，升级到更高 Minecraft
# 版本的主要技术障碍已经消除，1.21.1 仅作为当前发布基线，不作为长期锁定。
minecraft_version=1.21.1
yarn_mappings=1.21.1+build.3
loader_version=0.15.11

# Mod Properties
mod_version=1.1.4
maven_group=net.mccf
archives_base_name=MCConversationFramework

# Dependencies
fabric_version=0.116.15+1.21.1
modmenu_version=11.0.4

# Enabled parallel sync for Gradle 9.4+
org.gradle.tooling.parallel=true
